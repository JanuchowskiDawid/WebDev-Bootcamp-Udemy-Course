<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= category %> Products
    </title>
</head>

<body>
    <h1>
        <%= category %> products!
    </h1>
    <section>
        <% for(let product of products){ %>
            <ul>
                <li>
                    <%= product.name %> - <%= product.price %>$ <a href="/products/<%= product.id %> ">details</a>
                </li>
            </ul>
            <% } %>
    </section>
    <a href="/products/new">Add a product</a>
    <% if(category==='All' ){}else{%>
        <a href="/products">Home</a>
        <% } %>
</body>

</html>